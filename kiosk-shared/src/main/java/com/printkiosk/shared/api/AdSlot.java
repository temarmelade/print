package com.printkiosk.shared.api;

/** Место показа рекламы на киоске. */
public enum AdSlot {
    /** Крупный медиа-блок на главном экране (во время простоя). */
    HOME,
    /** Узкая полоса-баннер внизу всех экранов. */
    BANNER
}
