package com.printkiosk.shared.api;

/** Насколько инцидент мешает работе киоска. */
public enum IncidentSeverity {
    /** Печать невозможна — киоск фактически не работает. */
    DOWN,
    /** Киоск печатает, но скоро встанет (заканчиваются расходники). */
    WARNING
}
