package com.printkiosk.shared.api;

/** Роль сотрудника в админ-панели. */
public enum AdminRole {
    OWNER,       // владелец — всё, включая финансы и аналитику
    TECHNICIAN,  // техник — карта, статусы, расходники, перезагрузка (без выручки)
    SUPPORT      // поддержка — транзакции (возвраты) и статус киосков
}
